# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test / lint caches
.pytest_cache/
.ruff_cache/
.testmondata*
.coverage
.coverage.*
htmlcov/

# Indexed databases (and WAL/SHM sidecars)
*.db
*.db-wal
*.db-shm

# Retrieval eval cache, if --cache-dir points inside the repository
# (defaults to ~/.cache/indexter-eval, outside it)
eval-cache/
